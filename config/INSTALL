download ubuntu iso
download unetbootin
build usb key (unetbootin, ubuntu iso)
install + configure (partitioning,...)
    sound: http://www.madox.net/blog/projects/hp-probook-5310m-ubuntu-reference/
    mic: alsamixer, capture, F4 -> set to Front Microphone
apt-get install vim mc git
git clone config + install
dpkg --set-selections
apt-get -u dselect-upgrade + configure (dialogs, .rc,...)
update source list with ppa,...
    sudo add-apt-repository ppa:chromium-daily
    install + configure (server key,...)
list /opt + /usr/local + ~/bin + ~/local (AdobeAir, Balsamiq, Xmind,...)
    install + configure (software key,...)
network
    http://ubuntuforums.org/showthread.php?t=1344880

rsync home, DB,...
    stop services, close files (.swp)
    copy relevant .ssh parts
    configure mail
    copy pidgin config + log (got problems (? fs cache))
    config printer for Tiny printer + A4 format
    config xsane for Tiny scanner
        brscan2...deb
    rsync .eclipse, prune old dir, update (cannot create workspace if .eclipse synced from old one)
    config openoffice: A4 + french dict (http://extensions.services.openoffice.org/en/project/french-dictionary-classic-reform1990)

Xmind: http://www.xmind.net/downloads/
Eclipse:
    perl -pi.bak -e 's/home\/knu/home\/julien/g' $(fgrep -rl "home/knu" *)
    perl -pi.bak -e 's/home_knu/home_julien/g' $(fgrep -rl "home_knu" *)
Balsamiq: http://www.balsamiq.com/products/mockups/desktop
