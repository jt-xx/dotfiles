Host *
    IdentityFile ~/.ssh/id_dsa.T420s.priv
Host bazaar.launchpad.net
    IdentityFile ~/.ssh/id_rsa.launchpad.priv
    User julien-thewys
Host github.com
    IdentityFile ~/.ssh/id_dsa.github.priv
    User jt-xx
ControlMaster auto
ControlPath /tmp/ssh_mux_%h_%p_%r
ControlPersist yes

#AgentForwarding yes

# hostname aliases
# Host dev intranet backup
#   HostName %h.internal.example.com
#
# Host www* mail
#   HostName %h.example.com

# port forwarding: forward local 5433 to remote 5432
# Host db
#   LocalForward 5433 localhost:5432
# same
# Host api
#   LocalForward 8080 localhost:80

# gateway config: direct ssh to db through gateway
# Host db
#   HostName db.internal.example.com
#   ProxyCommand ssh gateway netcat -q 600 %h %p

# tunnelling config
# ProxyCommand corkscrew proxy.example.org 8080 %h %p

